<!DOCTYPE html>
<html>
<head>
  <title>Rabbit Overflow</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div id="nav">
  <nav>
    <ul>
      <% if current_user %>
        <a class="logged_in">Logged in as: <%= current_user.username %></a>
        <li><%= button_to "Log out", session_path(current_user.id), method: :delete %></li>
      <% else %>
          <li><%= button_to "Sign-up", new_user_path, method: :get %></li>
          <li><%= button_to "Login", new_session_path, method: :get %></li>
      <% end %>
        <li><%= button_to "Home", root_path %></li>
    </ul>
  </nav>
    <%= yield %>
  </div>
</body>
</html>
